server:
  port: 9090

spring:
  main:
    banner-mode: off
  #security:
  #  user: protoss
  #  password: 1234
  #  roles: user
  application:
    name: spring-lab-0-approach-1
  h2:
    console:
      enabled: 'true'
    datasource:
      username: cifoweb
      password: cifotest
      url: jdbc:h2:./test
      driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        # Once the JPA is created, change this value to 'update'
        ddl-auto: create-drop
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher